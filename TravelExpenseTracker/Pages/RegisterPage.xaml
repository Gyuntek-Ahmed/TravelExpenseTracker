<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TravelExpenseTracker.Pages.RegisterPage"
             xmlns:dx="http://schemas.devexpress.com/maui"
             xmlns:vm="clr-namespace:TravelExpenseTracker.ViewModels"
             x:DataType="vm:RegisterViewModel"
             Title="">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label" x:Key="appName">
                <Setter Property="FontSize" Value="28" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="HorizontalOptions" Value="Center" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto, Auto, Auto, *"
      RowSpacing="15">
        <VerticalStackLayout Grid.Row="0"
                         Margin="0,50,0,0"
                         HorizontalOptions="Center">
            <Label Style="{StaticResource appName}"
                   Text="Пътуване" />
            <Label Style="{StaticResource appName}"
                   Text="Разходи" />
            <Label Style="{StaticResource appName}"
                   Text="Проследяване" />
        </VerticalStackLayout>

        <Label Grid.Row="1"
               Text="Добре Дошли!"
               FontSize="22"
               HorizontalOptions="Center" />

        <VerticalStackLayout Grid.Row="2"
                             Spacing="20"
                             Padding="25">
            <dx:TextEdit PlaceholderText="Име"/>
            <dx:TextEdit PlaceholderText="Email"/>
            <dx:PasswordEdit PlaceholderText="Password"
                             IconColor="{dx:ThemeColor Primary}"
                             ErrorText="Невалидна Парола!"/>
            <dx:DXButton Content="Регистрация"
                         WidthRequest="250"
                         FontSize="20"
                         FontAttributes="Bold"/>
            <Label Text="Имате акаунт? Вход"
                   FontSize="18"
                   HorizontalOptions="Center"
                   TextColor="{AppThemeBinding Light={StaticResource Tertiary}, Dark={StaticResource Primary}}">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NavigateBackCommand}"/>
                </Label.GestureRecognizers>
            </Label>
        </VerticalStackLayout>
    </Grid>
</ContentPage>